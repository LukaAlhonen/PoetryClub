import { test, describe } from "vitest";

describe("Login unit tests", () => {
  test.todo("logs in with correct username and password")
  test.todo("logs in with incorrect username")
  test.todo("logs in wiht incorrect password")
})
