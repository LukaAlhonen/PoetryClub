import { describe, test } from "vitest";

describe("UnfollowButton unit tests", () => {
  test.todo("Follows an unfollowed author")
  test.todo("Follows a followed author, fails")
  test.todo("Checks that UnfollowButton is not rendered when not logged in")
})
