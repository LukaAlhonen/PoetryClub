import { test, describe } from "vitest";

describe("FolloweButton unit tests", () => {
  test.todo("follow button click on unwollowed author")
  test.todo("follow button click on followed author, --> fails")
  test.todo("Checks that FollowButton is not rendered when not logged in")
})
