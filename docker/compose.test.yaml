services:
  db:
    image: postgres
    restart: always
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=test
  redis:
    image: redis
    restart: always
    ports:
      - "6380:6379"
    command: redis-server /usr/local/etc/redis/redis-full.conf --requirepass test
    volumes:
      - redis-cache:/data
      - ./redis-full.conf:/usr/local/etc/redis/redis-full.conf

volumes:
  postgres-data:
  redis-cache:
